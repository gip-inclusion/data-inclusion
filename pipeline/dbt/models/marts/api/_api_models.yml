version: 2

models:
  - name: api_structure
    description: '{{ doc("api_structures") }}'
    config:
      alias: structure
  - name: api_service
    description: '{{ doc("api_services") }}'
    config:
      alias: service