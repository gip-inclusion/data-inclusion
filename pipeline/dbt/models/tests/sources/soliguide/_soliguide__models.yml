version: 2

models:
  - name: tests_soliguide__adresses
    tests:
      - check_adresse:
          config:
            severity: warn
    columns:
      - name: id
        tests:
          - unique
          - not_null
          - dbt_utils.not_empty_string

  - name: tests_soliguide__structures
    tests:
      - check_structure:
          config:
            severity: warn
    columns:
      - name: id
        tests:
          - unique
          - not_null
          - dbt_utils.not_empty_string
      - name: adresse_id
        tests:
          - not_null
          - relationships:
              to: ref('tests_soliguide__adresses')
              field: id

  - name: tests_soliguide__services
    tests:
      - check_service:
          config:
            severity: warn
    columns:
      - name: id
        tests:
          - unique
          - not_null
          - dbt_utils.not_empty_string
      - name: structure_id
        tests:
          - not_null
          - relationships:
              to: ref('tests_soliguide__structures')
              field: id
      - name: adresse_id
        tests:
          - not_null
          - relationships:
              to: ref('tests_soliguide__adresses')
              field: id
