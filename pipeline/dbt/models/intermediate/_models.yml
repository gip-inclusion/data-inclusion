version: 2

models:
  - name: int__adresses
    tests:
      - check_adresse:
          include:
            - _di_surrogate_id
          config:
            severity: warn
            store_failures: true

  - name: int__services
    tests:
      - check_service:
          include:
            - _di_surrogate_id
          config:
            severity: warn
            store_failures: true

  - name: int__structures
    tests:
      - check_structure:
          include:
            - _di_surrogate_id
          config:
            severity: warn
            store_failures: true

  - name: int__plausible_personal_emails
  - name: int__deprecated_sirets

  - name: int__adresses_geocoded
  - name: int__enhanced_services
  - name: int__enhanced_structures
