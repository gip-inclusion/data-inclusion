version: 2

models:
  - name: int_dora__adresses
    tests:
      - check_adresse:
          config:
            severity: warn

  - name: int_dora__services
    tests:
      - check_service:
          config:
            severity: warn

  - name: int_dora__structures
    tests:
      - check_structure:
          config:
            severity: warn