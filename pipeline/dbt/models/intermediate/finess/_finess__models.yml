version: 2

models:
  - name: int_finess__adresses
    tests:
      - check_adresse:
          config:
            severity: warn

  - name: int_finess__structures
    tests:
      - check_structure:
          config:
            severity: warn