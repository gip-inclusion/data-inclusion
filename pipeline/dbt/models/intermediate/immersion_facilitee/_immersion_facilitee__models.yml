version: 2

models:
  - name: int_immersion_facilitee__adresses
    tests:
      - check_adresse:
          config:
            severity: warn

  - name: int_immersion_facilitee__structures
    tests:
      - check_structure:
          config:
            severity: warn